# macOS platform contract

Status: authoritative platform summary

The arm64 DMG packages the common Qt frontend and separate Gamma9001 AMY
service. Platform discovery and packaging stay outside portable application
policy, and external control must retain the same observable contracts as the
other hosts.

Executable build and release details are in `../../../qt_frontend/INSTALL.md`.
