# Windows platform contract

Status: authoritative platform summary

The native x86_64 package keeps Qt and Gamma9001 AMY in separate processes and
uses a private per-run named pipe. Windows-native MIDI discovery and transport
belong in platform adapters; no Windows branch enters portable musical policy.

Build, package and current validation details are in
`../../../qt_frontend/docs/WINDOWS_NATIVE.md`.
