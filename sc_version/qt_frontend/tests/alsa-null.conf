# Headless CI playback target. Native AMY still starts its real audio callback,
# but ALSA discards samples instead of requiring a hardware card on the runner.
pcm.!default {
    type null
}

ctl.!default {
    type null
}
