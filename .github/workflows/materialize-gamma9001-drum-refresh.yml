name: Materialize Gamma9001 drum refresh

on:
  push:
    branches:
      - feature/gamma9001-drum-pattern-refresh
    paths:
      - '.github/workflows/materialize-gamma9001-drum-refresh.yml'

permissions:
  contents: write

jobs:
  materialize:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: feature/gamma9001-drum-pattern-refresh

      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: '3.12'

      - name: Verify and materialize reviewed payload
        run: |
          python amysynth_version/design/rhythm_rework/new_patterns/materialize_gamma9001_drum_refresh.py --check
          python amysynth_version/design/rhythm_rework/new_patterns/materialize_gamma9001_drum_refresh.py

      - name: Install portable test dependencies
        working-directory: amysynth_version/qt_frontend
        run: python -m pip install -r requirements-test.txt

      - name: Diagnose rhythm/fill meter compatibility
        working-directory: amysynth_version/qt_frontend
        run: |
          python - <<'PY'
          import sys
          from itertools import combinations
          from pathlib import Path
          sys.path.insert(0, str(Path('code').resolve()))
          from amy_transport import AmySerialClient
          from drum_patterns import load_drum_pattern_catalog
          catalog = load_drum_pattern_catalog(Path('music/drums'))
          for rhythm in catalog.rhythms.values():
              beats_per_bar = int(rhythm.meter.split('/', 1)[0])
              for count in range(1, len(rhythm.fills) + 1):
                  for order_tuple in combinations(range(len(rhythm.fills)), count):
                      occurrences = AmySerialClient._fill_occurrences(list(order_tuple), rhythm.fills)
                      for fill, start_beat in occurrences:
                          if start_beat > beats_per_bar:
                              print('METER_MISMATCH', rhythm.rhythm_id, rhythm.meter, fill.fill_id, fill.allowed_start_beats, start_beat)
          PY

      - name: Run drum and catalogue provenance tests
        working-directory: amysynth_version/qt_frontend
        run: |
          python tests/test_drum_patterns.py
          python tests/test_catalogue_provenance.py

      - name: Record completion status
        run: |
          python - <<'PY'
          from pathlib import Path
          p = Path('amysynth_version/design/rhythm_rework/new_patterns/CODEX_HANDOVER_GAMMA9001_DRUM_PATTERN_REFRESH.md')
          text = p.read_text(encoding='utf-8')
          text = text.replace(
              'Status: **data authored and validated; materialization into the canonical runtime JSON is the next bounded step**  ',
              'Status: **canonical runtime data materialized and targeted validation passed**  ',
              1,
          )
          marker = '\n## 13. Definition of done\n'
          evidence = '''\n## 12a. Automated materialization evidence\n\nThe branch-local materialization job SHA-verifies the staged payload, writes the canonical runtime JSON, updates manifest/provenance, then runs `tests/test_drum_patterns.py` and `tests/test_catalogue_provenance.py` before committing. The temporary transport files and workflow are removed in the subsequent cleanup commit.\n'''
          if evidence not in text:
              text = text.replace(marker, evidence + marker, 1)
          p.write_text(text, encoding='utf-8')
          PY

      - name: Commit canonical data
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add \
            amysynth_version/qt_frontend/music/drums/drum_activity_timing.json \
            amysynth_version/qt_frontend/music/catalogue_provenance.json \
            amysynth_version/design/rhythm_rework/new_patterns/canonical_drum_data_manifest.json \
            amysynth_version/design/rhythm_rework/new_patterns/CODEX_HANDOVER_GAMMA9001_DRUM_PATTERN_REFRESH.md
          git diff --cached --check
          git commit -m 'Materialize Gamma9001 drum activity refresh'
          git push origin HEAD:feature/gamma9001-drum-pattern-refresh
